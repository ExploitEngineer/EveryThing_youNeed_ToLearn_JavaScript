// INFO: While Loop

console.log("Program Started");

let num = 0;

while (num < 5) {
    console.log(`The value of num is ${num}`);
    num += 1;
};


let numbers = 1;

while (numbers < 101) {
    console.log(`The value of numbers is ${numbers}`);
    numbers++;
};


const arr = ['michael', 'stephen', 'mary', 'john', 'chris'];
let i = 0;

while (i < arr.length) {
    console.log(arr[i]);
    i++;
};
